package tn.iset.investplatformpfe.Entity;



public enum MessageType {
    TEXT,      // Message texte simple
    IMAGE,     // Message avec image(s)
    DOCUMENT   // Message avec document(s) (PDF, Word, etc.)
}
